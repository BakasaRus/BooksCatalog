{% extends 'base.html.twig' %}

{% block title %}Список авторов{% endblock %}

{% block body %}
<div class="container">
    <h1>Список авторов</h1>

    <ul>
        {% for author in authors %}
            <li>
                {{ author.fullname }}
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
